import { Dictionary } from "./Dictionary";
/**
 * Represents a dictionary that is sorted alphabetically.
 *
 * @template TKey
 * The type of the keys.
 *
 * @template TValue
 * The type of the values.
 */
export declare class AlphabeticalDictionary<TKey, TValue> extends Dictionary<TKey, TValue> {
    /**
     * Initializes a new instance of the {@link AlphabeticalDictionary `AlphabeticalDictionary<TKey, TValue>`} class.
     */
    constructor();
    /**
     * Initializes a new instance of the {@link AlphabeticalDictionary `AlphabeticalDictionary<TKey, TValue>`} class.
     *
     * @param entries
     * The entries to add.
     */
    constructor(entries: Iterable<readonly [TKey, TValue]> | Dictionary<TKey, TValue>);
    /**
     * @inheritdoc
     */
    get Entries(): Array<[TKey, TValue]>;
}
