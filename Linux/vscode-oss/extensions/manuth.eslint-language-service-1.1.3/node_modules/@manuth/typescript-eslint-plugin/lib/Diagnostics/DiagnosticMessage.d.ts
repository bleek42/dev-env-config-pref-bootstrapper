import type { DiagnosticCategory, DiagnosticMessageChain, SourceFile } from "typescript/lib/tsserverlibrary";
import { Plugin } from "../Plugin";
import { Diagnostic } from "./Diagnostic";
/**
 * Represents a diagnostic-message.
 */
export declare class DiagnosticMessage extends Diagnostic {
    /**
     * The message of the diagnostic.
     */
    private message;
    /**
     * Initializes a new instance of the {@link DiagnosticMessage `DiagnosticMessage`} class
     *
     * @param plugin
     * The plugin of the diagnostic.
     *
     * @param file
     * The file of the diagnostic.
     *
     * @param message
     * The message of the diagnostic.
     *
     * @param category
     * The category of the diagnostic.
     */
    constructor(plugin: Plugin, file: SourceFile, message: string | DiagnosticMessageChain, category?: DiagnosticCategory);
    /**
     * @inheritdoc
     */
    get Message(): string | DiagnosticMessageChain;
}
