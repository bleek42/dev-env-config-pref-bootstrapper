# #!/usr/bin/bash

export PNPM_GLOBAL=/c/Users/brand/AppData/Local/pnpm/global/5

# if command -v nvm >/dev/null 2>&1; then
# 		if [[ $WT_PROFILE_ID = "{7ea1a638-2230-4e4f-a668-5376c63537cc}" ]]; then
#         NODE_PATH='eval'
#         NODE_EXE=
# 			eval "$(nvm use latest)"
# 		else
# 			eval "$(nvm use lts)"
# 		fi
# 	fi
# fi
