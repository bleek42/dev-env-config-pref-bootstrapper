import { JSONObjectBase } from "../Utilities/JSONObjectBase";
import { ICollection } from "./ICollection";
/**
 * Provides the functionality to store collections of items.
 *
 * @template TKey
 * The type of the keys of the collection.
 *
 * @template TValue
 * The type of the values of the collection.
 */
export declare abstract class Collection<TKey, TValue> extends JSONObjectBase<any> implements ICollection<TKey, TValue> {
    /**
     * Initializes a new instance of the {@link Collection `Collection<TKey, TValue>`} class.
     */
    constructor();
    /**
     * @inheritdoc
     */
    get Count(): number;
    /**
     * @inheritdoc
     */
    abstract get Entries(): Array<[TKey, TValue]>;
    /**
     * @inheritdoc
     */
    get Keys(): TKey[];
    /**
     * @inheritdoc
     */
    get Values(): TValue[];
    /**
     * @inheritdoc
     */
    abstract Clear(): void;
    /**
     * Returns an object representing this collection.
     *
     * @returns
     * An object representing this collection.
     */
    abstract ToJSON(): any;
}
