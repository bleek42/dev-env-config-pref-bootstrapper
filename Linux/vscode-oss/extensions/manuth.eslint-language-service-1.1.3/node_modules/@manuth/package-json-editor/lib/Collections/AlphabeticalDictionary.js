"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AlphabeticalDictionary = void 0;
const Dictionary_1 = require("./Dictionary");
/**
 * Represents a dictionary that is sorted alphabetically.
 *
 * @template TKey
 * The type of the keys.
 *
 * @template TValue
 * The type of the values.
 */
class AlphabeticalDictionary extends Dictionary_1.Dictionary {
    /**
     * Initializes a new instance of the {@link AlphabeticalDictionary `AlphabeticalDictionary<TKey, TValue>`} class.
     *
     * @param args
     * The passed arguments.
     */
    constructor(...args) {
        super();
        if (args.length > 0) {
            this.AddRange(...args);
        }
    }
    /**
     * @inheritdoc
     */
    get Entries() {
        let result = Array.from(super.Entries);
        result.sort((a, b) => {
            let args = [a, b].map((value) => `${value[0]}`.toLocaleLowerCase());
            return args[0].localeCompare(args[1], []);
        });
        return result;
    }
}
exports.AlphabeticalDictionary = AlphabeticalDictionary;
//# sourceMappingURL=AlphabeticalDictionary.js.map